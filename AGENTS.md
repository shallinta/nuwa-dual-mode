# AGENTS

## Agent skills

### Issue tracker

GitHub Issues，PR 不作为 triage 需求入口。详见 `docs/agents/issue-tracker.md`。

### Triage labels

`needs-triage` / `needs-info` / `ready-for-agent` / `ready-for-human` / `won't-fix`。详见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文 — 根目录 `CONTEXT.md` + `docs/adr/`。详见 `docs/agents/domain.md`。
